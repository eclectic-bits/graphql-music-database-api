datasource db {
  provider = "sqlite"
  url      = "file:./chinook.db"
}

generator client {
  provider = "prisma-client-js"
}

model Genre {
  genreId Int    @id @default(autoincrement())
  name    String

  @@map(name: "Genres")
}
